<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../../public/style/books-style/index-style.css">
    <title>Books Search Results</title>
</head>

<body>

    <h1>You Have These <span><%= y %></span> Books</h1><!-- display count -->

    <% x.forEach((value,index) =>{ %>
    <div id="bookDiv">
        <img src="<%= value.image_url %>">
        <h2>
            <%= value.title %>
        </h2>
        <h4 id="author">
            <%= value.author %>
        </h4>
        <button id="detailsBtn" type="submit" value="Show Details"
            onclick="location.href = `/books/<%= value.id %>`;">Show Details</button>

    </div>
    <% }); %>
    <a href="/book">Back To Home Page</a>
</body>

</html>